<%= form_for(@study_spot) do |f| %>
  <% if @study_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_spot.errors.count, "error") %> prohibited this study_spot from being saved:</h2>

      <ul>
      <% @study_spot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
